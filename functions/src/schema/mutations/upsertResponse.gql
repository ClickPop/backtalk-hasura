mutation upsertResponse($input: responses_insert_input!) {
  insert_responses_one(
    object: $input
    on_conflict: {
      constraint: responses_pkey
      update_columns: [response_content, response_option_id]
    }
  ) {
    wallet
    question_id
    response_content
  }
}
