mutation upsertResponses($input: [responses_insert_input!]!) {
  insert_responses(
    objects: $input
    on_conflict: {
      constraint: responses_pkey
      update_columns: [response_content, response_option_id]
    }
  ) {
    returning {
      wallet
      question_id
      response_content
    }
  }
}
